version: "2"

run:
  timeout: 5m

linters:
  default: standard
  enable:
    - misspell
  exclusions:
    generated: lax
    paths:
      - zz_generated\\.deepcopy\\.go
      - internal/controller/envtest

formatters:
  enable:
    - gofmt
