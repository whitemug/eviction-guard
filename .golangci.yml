version: "2"

run:
  timeout: 5m

linters:
  default: standard
  enable:
    - misspell
  exclusions:
    generated: lax
    paths:
      - zz_generated\\.deepcopy\\.go

formatters:
  enable:
    - gofmt
